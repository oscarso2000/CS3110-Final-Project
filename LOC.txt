reproduce.ml is used to convert strings into emojis. As it matches well over 1000 emojis, each on their own line, we do not count that towards our LOC. We included everything else in .ml and .mli files since nothing else was dead code or trivial or brute force. 